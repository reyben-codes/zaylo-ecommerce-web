@extends('layouts.auth')
@section('title', 'Reset Password · ZAYLO')
@push('styles')<style>body{margin:0;background:#faf7f2;font-family:Inter,sans-serif}.auth-simple{max-width:520px;margin:10vh auto;background:#fff;padding:56px;border:1px solid #ece4db}.auth-simple h1{font-family:'Playfair Display',serif}.auth-simple label{display:block;margin:24px 0}.auth-simple input{width:100%;box-sizing:border-box;padding:14px;border:1px solid #ddd}.auth-simple button{padding:14px 24px;background:#1a1714;color:#fff;border:0}.error{color:#b42318}</style>@endpush
@section('content')<main class="auth-simple"><h1>Forgot your password?</h1><p>Enter your account email and we’ll send a secure reset link.</p>@if(session('status'))<p>{{ session('status') }}</p>@endif @if($errors->any())<p class="error">{{ $errors->first() }}</p>@endif<form method="POST" action="{{ route('password.email') }}">@csrf<label>Email address<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><button>Send reset link</button></form></main>@endsection
